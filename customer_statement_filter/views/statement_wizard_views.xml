<odoo>
    <record id="view_customer_statement_wizard_form" model="ir.ui.view">
        <field name="name">customer.statement.wizard.form</field>
        <field name="model">customer.statement.wizard</field>
        <field name="arch" type="xml">
            <form string="Generate Statements">
                <sheet>
                    <group>
                        <group string="Statement Period">
                            <field name="date_from"/>
                            <field name="date_to"/>
                        </group>
                        <group string="Options">
                            <field name="account_id" options="{'no_create': True}"/>
                            <field name="exclude_zero_balance"/>
                            <field name="statement_type" widget="radio" options="{'horizontal': true}"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Select Customers">
                            <field name="partner_ids">
                                <list>
                                    <field name="name"/>
                                    <field name="email"/>
                                    <field name="total_due" sum="Total"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <footer>
                    <button name="action_print_statements" string="Print PDF" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_customer_statement_wizard" model="ir.actions.act_window">
        <field name="name">Customer Statements</field>
        <field name="res_model">customer.statement.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_customer_statement_wizard" 
              name="Customer Statements" 
              parent="account.menu_finance_receivables" 
              action="action_customer_statement_wizard" 
              sequence="110"/>
</odoo>
