<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_move_form_customer_vendor_type" model="ir.ui.view">
        <field name="name">account.move.form.customer.vendor.type</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="attributes">
                <attribute name="domain">[('is_vendor', '=', True)] if move_type in ('in_invoice', 'in_refund', 'in_receipt') else ([('is_customer', '=', True)] if move_type in ('out_invoice', 'out_refund', 'out_receipt') else [])</attribute>
                <attribute name="context">{
                    'default_is_vendor': move_type in ('in_invoice', 'in_refund', 'in_receipt'),
                    'default_is_customer': move_type in ('out_invoice', 'out_refund', 'out_receipt'),
                    'res_partner_search_mode': 'supplier' if move_type in ('in_invoice', 'in_refund', 'in_receipt') else 'customer',
                }</attribute>
            </xpath>
        </field>
    </record>

</odoo>
